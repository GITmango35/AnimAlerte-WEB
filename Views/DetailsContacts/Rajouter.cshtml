@inject Microsoft.AspNetCore.Mvc.Localization.IViewLocalizer localizer
@model AnimAlerte.Models.DetailsContact

@{ ViewData["Title"] = localizer["AddContact"]; }

<h1>@localizer["AddContact"] @ViewBag.contact ?</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create2">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="form-group">
            <label asp-for="NomUtilisateurCreateur" class="control-label"></label>
            <select asp-for="NomUtilisateurCreateur" class="form-control"></select>
        </div>
        <div class="form-group">
            <label asp-for="NomUtilisateurFavoris" class="control-label"></label>
            <select asp-for="NomUtilisateurFavoris" class="form-control" asp-items="ViewBag.NomUtilisateurFavoris"></select>
        </div>
           
            <div class="form-group">
               
                @*<input type="submit" value="@localizer["AddContact"]" class="btn btn-primary" />*@
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">@localizer["BackToList"]</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
